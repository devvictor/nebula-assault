{
  "name": "nebula-assault",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Web-first arcade space shooter, built to port to iOS and Android.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "phaser": "^4.2.1"
  }
}
